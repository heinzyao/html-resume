[project]
name = "html-resume"
version = "0.1.0"
description = "Static resume generator: YAML data + Jinja2 template → HTML/PDF"
requires-python = ">=3.11"
dependencies = [
    "jinja2>=3.1",
    "pyyaml>=6.0",
]

[project.optional-dependencies]
pdf = ["playwright>=1.40"]

